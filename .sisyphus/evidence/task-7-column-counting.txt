Task 7: Test Failures in test_tabs.lua and test_API.lua
========================================================

ANALYSIS OF FAILURES:

test_API.lua (2 failures):
---------------------------
ISSUE: pathToFile being expanded to absolute path in config.lua
- Line 47: Expected `no-neck-pain-left.norg` but got `/Users/k/Documents/no-neck-pain.nvim/no-neck-pain-left.norg`
- Line 192: Same issue for overrides test

ROOT CAUSE:
- config.lua:359 calls vim.fn.getcwd() during parse_deprecated_scratchPad
- This expands pathToFile to absolute path at setup() time
- Task 4 fixed ui.lua but missed that config.lua also does path construction
- Tests expect relative paths in config, expansion should happen at USE time (ui.lua)

SOLUTION:
- Remove vim.fn.getcwd() expansion from config.lua:359
- Keep pathToFile as relative path during setup
- Let ui.lua handle expansion when actually creating scratchpad (already fixed in Task 4)

test_tabs.lua (9 failures):
---------------------------
After analyzing the failures:

1. Line 91: "side buffers coexist on many tabs"
   - Expected only main window {1003} on tab 2
   - Got {1004, 1003, 1005} meaning side buffers were created
   - Test does NOT call .nnp() on tab 2, expects no side buffers
   - Line 93 DOES call .nnp() and expects side buffers to be created then

2. Lines 179, 201, 239, 286, 320, 399: All TabEnter/tabnew tests
   - Expected state.enabled = true
   - Getting state.enabled = false
   - These use init_auto_open.lua which sets enableOnTabEnter=true
   - Plugin not auto-enabling on TabEnter as expected

3. Lines 548, 622: State structure tests
   - Expected state.tabs[X] = vim.NIL
   - Getting state.tabs[X] = table (actual data)
   - Plugin is creating tab entries when it shouldn't

CONCLUSION: Two separate issues
1. Config pathToFile expansion (test_API.lua) - FIX in config.lua
2. Auto-enable/state management (test_tabs.lua) - Likely related to debouncer or autocmd logic


PROGRESS UPDATE:
===============

test_API.lua: ✅ FIXED (0 failures)
--------------------------------------
ISSUE: pathToFile being expanded to absolute path in config.lua:359
FIX: Removed vim.fn.getcwd() call in parse_deprecated_scratchPad()
- Now constructs relative path: "no-neck-pain-{side}.norg"
- Path expansion happens at runtime in ui.lua (Task 4 fix)
- Tests now pass completely

test_tabs.lua: ❌ STILL FAILING (7-10 failures)
-------------------------------------------------
ATTEMPTS:
1. Added tab registration in TabEnter autocmd
   - Lines 202-206 in main.lua
   - Registers tab when plugin is globally enabled
   - REGRESSION: Caused toggle() to disable instead of enable

2. Fixed toggle() logic to check for side buffers
   - Lines 16-23 in main.lua
   - Now checks if tab has side buffers (left/right), not just registration
   - PARTIAL: Some tests still fail

REMAINING ISSUES:
1. VimEnter auto-enable not working (enableOnVimEnter=true)
   - Tests expect state.enabled=true after VimEnter
   - Getting state.enabled=false
   - Affects lines 179, 201, 239, 286, 320, 399

2. Auto-initialization of new tabs inconsistent
   - Line 91, 699: Expected side buffers but getting only main window
   - WinEnter/WinClosed autocmd should trigger auto-init (main.lua:220-226)
   - But auto-init not firing reliably

3. Tab state registration timing
   - Lines 548, 622: Expected nil but getting table
   - Tabs being registered when they shouldn't be

ROOT CAUSE ANALYSIS:
- TabEnter autocmd registration (my fix) causes timing issues
- Toggle logic needs to distinguish:
  * Tab not registered (enable)
  * Tab registered but no buffers (enable)
  * Tab registered with buffers (disable)
- VimEnter auto-enable path may be broken independently

NEXT STEPS:
- Revert TabEnter registration changes
- Debug VimEnter auto-enable flow
- Understand when tabs SHOULD vs SHOULDN'T be registered
- May need to check debouncer timing issues
